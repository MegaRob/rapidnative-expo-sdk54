import type { FirebaseApp } from "@firebase/app";

/** Platform implementation: `initAppCheck.web.ts` / `initAppCheck.native.ts` (Metro). */
export declare function initAppCheckAfterFirebase(app: FirebaseApp): void;
