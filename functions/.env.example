# Local Firebase emulator / `firebase emulators:start` only — copy to `functions/.env` (gitignored).
# Production: use `firebase functions:secrets:set STRIPE_SECRET_KEY` (or legacy functions.config stripe.secret).

STRIPE_SECRET_KEY=
PLATFORM_FEE_PERCENT=5
